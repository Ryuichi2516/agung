= render "modules/header"

%main
  .accountContent
    %h2.title 会員情報登録
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = render "users/shared/error_messages", resource: resource
      .accountForm
        .required &ensp;必須
        .fieldEmail
          = f.label :email, "メールアドレス"
          = f.email_field :email, autofocus: true, autocomplete: "email", class: "fieldEmail__input", placeholder: "PC・携帯どちらでも可"
        .required &ensp;必須
        .fieldPassword
          = f.label :password, "パスワード"
          = f.password_field :password, autocomplete: "new-password", class: "fieldPassword__input", placeholder: "7文字以上の半角英数字"
        .fieldGuide &ensp;＊ 半角英数字７文字以上で設定して下さい
        .required &ensp;必須
        .fieldPassword
          = f.label :password_confirmation, "再度パスワード"
          = f.password_field :password_confirmation, autocomplete: "new-password", class: "fieldPassword__input", placeholder: "7文字以上の半角英数字"
        .checkField
          = check_box_tag :check
          = label_tag :check, "パスワードを表示する", class: "checkbox"
        .sign_up
          = f.submit "登録内容を確認する", class:"sign_up__btn"

= render "modules/footer"